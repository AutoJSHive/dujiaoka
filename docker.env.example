# Docker 部署环境变量配置
# 复制此文件为 .env 并修改配置

# 数据目录
DATA_DIR=/data

# 独角数卡数据库
DB_DATABASE=dujiaoka
DB_USERNAME=dujiaoka
DB_PASSWORD=change_me_to_random_string

# ============================================
# TokenPay 区块链支付配置
# ============================================

# API 密钥（独角数卡和 TokenPay 共享，自动生成或手动设置）
TOKENPAY_API_TOKEN=your_random_api_token_here

# TRON 收款地址（TRX、USDT-TRC20）
TOKENPAY_TRON_ADDRESS=TYourTronAddressHere

# EVM 收款地址（ETH、BSC、Polygon 等 ERC20）
TOKENPAY_EVM_ADDRESS=0xYourEvmAddressHere

# TronGrid API Key（申请地址: https://www.trongrid.io/dashboard/keys）
TOKENPAY_TRONGRID_KEY=your-trongrid-api-key

# TokenPay 外网地址（用于生成支付页面链接）
# 注意：独角数卡内部通过 127.0.0.1:5000 通信，此地址仅用于用户访问支付页面
TOKENPAY_WEBSITE_URL=https://your-domain.com

# 基础货币（CNY、USD、EUR、GBP、AUD、HKD、TWD、SGD）
TOKENPAY_BASE_CURRENCY=CNY

# ============================================
# Telegram 通知（可选）
# ============================================

# 管理员 Telegram ID（获取方式: 给 @QueryCoinBot 发送 /tgid）
TOKENPAY_TG_ADMIN_ID=0

# Telegram Bot Token（从 @BotFather 创建机器人获取）
TOKENPAY_TG_BOT_TOKEN=
